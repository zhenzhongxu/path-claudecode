---
globs: ["**/*"]
description: "World model — self-model (capabilities, calibration)"
---

# Self-Model

What the agent knows about itself: capabilities, limitations, error patterns, calibration.

Currently at step 0 — no self-model yet. This file will be populated as the agent accumulates experience and feedback about its own performance.
